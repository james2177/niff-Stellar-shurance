/** DOM id for the Network section on the settings page (deep-link from mismatch overlay). */
export const SETTINGS_NETWORK_SECTION_ID = 'settings-network'

/** Persisted wallet type after a successful connect (session restore on reload). */
export const LAST_WALLET_ID_STORAGE_KEY = 'niffyinsure:lastWalletId'
